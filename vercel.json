{
  "version": 2,
  "buildCommand": "ROLLUP_NATIVE_EXTENSIONS=false npm run build",
  "installCommand": "npm install --no-optional --no-audit --no-fund",
  "framework": "sveltekit",
  "outputDirectory": "build",
  "github": {
    "silent": true
  },
  "env": {
    "ROLLUP_NATIVE_EXTENSIONS": "false"
  }
}
